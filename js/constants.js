// body measures
const bodyLength = 6;
const bodyWidth = 4;
const bodyHeight = 3;

// back measures
const backLength = 3;
const backWidth = 1.5;
const backHeight = 3;

// head measure
const headEdgeLength = 1.5;

// antena measures
const antenaRadius = 0.1;
const antenaHeight = 0.7;

// eye measure
const eyeRadius = 0.2;

// arm measures
const armLength = 1.5;
const armWidth = 1.5;
const armHeight = 3;

// forearm measures
const forearmLength = 1;
const forearmWidth = 5.5;
const forearmHeight = 1;

// waist measure
const waistLength = 5;
const waistWidth = 5.5;
const waistHeight = 2;

// abdomen measures
const abdomenLength = 4;
const abdomenWidth = 5.5;
const abdomenHeight = 1;

// wheels measures
const wheelRadius = 1;
const wheelHeight = 1;

// pipe measures
const pipeRadius = 0.15;
const pipeHeight = 3;

// thigh measures
const thighLength = 0.7;
const thighWidth = 0.7;
const thighHeight = 2;

// leg measures
const legLength = 1.5;
const legWidth = 2;
const legHeight = 5;

// foot measures
const footLength = legLength + wheelHeight;
const footWidth = 2;
const footHeight = 0.7;

// trailer measures
const trailerLength = 6;
const trailerWidth = 16;
const trailerHeight = 8;

// trailer piece
const trailerPieceHeight = 3;
const trailerPieceRadius = 1;

// speed
const moveSpeed = 0.05;